# Collection: `about`

- **Source:** `content/over-indicium.md` · **Type:** `page` · **Schema:** no
- **Used by:** none — **unused / dead collection.**

## Status
- The source file `content/over-indicium.md` does not exist.
- The "Over Indicium" page (`pages/over-indicium.vue`) renders **hardcoded** content (carousel images, stats, text) and never queries this collection.

## Notes
- Candidate for cleanup: either delete the dead collection or migrate the hardcoded page content into an actual `over-indicium.md` and make the page a shim (aligns with the "pages are shims" goal — see `references/architecture.md`).
