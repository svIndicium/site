# 02 — Consolidation (duplication with clear payoff)

- TD-010 [MED] components/NavMobile.vue:66-160 (457-line slide-in nav; near-identical level-2 `:88-124` / level-3 `:125-155` blocks) vs components/MenuItem.vue (recursive renderer used by NavDesktop) + own `navigation-mobile` fetch key (`NavMobile.vue:10`) vs `navigation` (`NavDesktop.vue:3`) + visibility stacked across `layouts/default.vue:4,24-32`, `NavDesktop.vue:80-83`, `NavMobile.vue:442-453` (unreachable ≥945px rule), `NavLogo.vue:57-62` at inconsistent 768/944/945px. → One recursive item component, one fetch key, one breakpoint source (see epic E3 in `01-high-priority.md`). Impact M / Effort M → acceptance: - [ ] nav change needs one edit; 769–944px range deterministic
- TD-011 [LOW] components/NavLogo.vue:8-15 vs components/LogoElement.vue:5-13 vs pages/Links.vue:14-22 — same svg + INDICIUM/ICT wordmark DOM with three independent sizing cascades, plus `router.push`-on-div (`NavLogo.vue:9-10`) and inert cross-component restyle attempts (`NavMobile.vue:202-211` scoped rules that can't reach child internals without `:deep`). → One brand component with size/click props; delete inert rules. Impact S / Effort M → acceptance: - [ ] single wordmark source; Links reclaims ~45 lines
- TD-012 [LOW] pages/partners/index.vue:16-29,92-108 vs pages/partners/[partner].vue:16-38,44-60 — copy-pasted partner hero (logo/link/CTA, ContentRenderer, JobOffers tail, `.details` grid CSS) already drifted, plus per-page `partnerSlug` job queries (`index.vue:8-9`, `[partner].vue:11-12`) while `composables/usePartners.ts:1-15` returns data-only refs with no status/error. → One `PartnerDetails` section component + `usePartnerJobOffers(slug)`; surface status/error from usePartners. Impact S / Effort M → acceptance: - [ ] hero edited once; callers distinguish loading/empty/error
- TD-013 [LOW] pages/Bestuur.vue:4-6 (`order(boardNumber,DESC).first()`) vs pages/besturen.vue:7-13 (query-all + `Math.max`) — "current = highest boardNumber" implemented twice with duplicated comments; a future-dated board silently desyncs the two routes. → Shared `useBoards()` (allBoards + derived currentBoard) per the usePartners pattern. Impact S / Effort S → acceptance: - [ ] one derivation; both pages consume it
- TD-014 [MED] composables/useAgenda.ts:1-21 (`export *` shim + non-composable `getAgendaApiKey`, self-admitted "backward compatibility") vs utils/agenda.ts — every agenda symbol has two auto-import candidates; importers split (`ActivityCalendar.vue:26`, `Activiteiten.vue:3`, tests via composables; `AgendaCalendarButton.vue:6` via utils). → Move `getAgendaApiKey` into utils, migrate 4 call sites, delete shim. Impact S / Effort S → acceptance: - [ ] one canonical import path; shim deleted
- TD-015 [MED] utils/agenda.ts:188-203 (`AGENDA_DEFAULT_LOCATION_ENTRIES`, "keep in sync with the YML") vs content/agenda-locations.yml:3-29 — fallback omits Cambridgebar/cb + HL7 entries and queries Utrecht venues as "… Leuven", live as degraded-path fallback (`ActivityCalendar.vue:81-86`) and helper defaults. → Single source of truth (generate fallback from YML or require entries from callers) + parity check. Impact M / Effort S → acceptance: - [ ] no wrong-city Maps links; fallback can't silently diverge
- TD-016 [MED] content/contact.yml:1-4 vs content/footer.yml:15-19,22-27 vs content/links.yml + hardcoded mailtos (`pages/Contact.vue`) and hardcoded socials (`components/SocialSidebar.vue`) — one address/room/email maintained in 3 shapes; Instagram/GitHub URLs already spell-drifted between footer and links. → One shared org/contact + socials source consumed by footer, contact page, links page, sidebar; normalize URLs. Impact S-M / Effort M → acceptance: - [ ] relocation/rebrand = one edit; URLs identical everywhere
- TD-017 [MED] content/boards/*.md (title "2025 2026" vs year "2025-2026" vs filename; description duplicated as italic body line in all 9 files; only boardNumber/year/body rendered) + dead `commissions` schema field (`content.config.ts:114`, set nowhere) + responsibilities free-text drift vs commissie titles ("Developmentcommissie" vs "Dev Commissie", "Reiscommissie" vs "Reis Commissie") + filename-prefix/order-field double sort (`content/commissies/1-..-7-*.md`). → Keep year as single key aligned to filename, auto-derive ordinal description, delete `commissions`, align responsibility values to commissie slugs, keep one sort source. Impact M / Effort S-M → acceptance: - [ ] new board year = one file, no re-typed copy; names match /commissies pages
- TD-018 [MED] content/partners/** (8 files: `chipsoft/index.md:3-5,15-16`, `vibe-academy/*`, `axians/*`, `hogeschool-utrecht/*`, offer files) — `description` frontmatter duplicated verbatim as body lead (typo "carriere" already copied twice). → Canonical location = body, description derived/removed; document the rule. Impact M / Effort S → acceptance: - [ ] one copy per sentence; convention documented
- TD-019 [MED] assets/css/variables.css:92-146 (OS-dark block) vs :149-202 (attribute/radio block) — ~45-line dark palette copy-pasted; already drifted (`--indi-success-raw` triplet-vs-color contract broken at :131/:188; warning/error/grey raws kept at light values while rgb twins darkened). → Generate both blocks from one palette definition; enforce raw-triplet rule. Impact M / Effort M → acceptance: - [ ] one palette source; no latent contract break
- TD-020 [MED] components/PartnerCarousel.vue (dead standalone marquee, zero consumers, not Studio-global) vs components/content/HomePartners.vue:44-63,139-162 (inline re-implementation: doubled list, track, keyframes, hover-pause, reduced-motion). → Delete PartnerCarousel; keep HomePartners marquee as sole owner (extract shared ticker only if a second consumer appears). Impact M / Effort S → acceptance: - [ ] one marquee implementation; dead file gone
- TD-021 [MED] components/content/HomeImageCarousel.vue:6-26 (ClientOnly + LazyImageCarousel + fallback img, hardcoded fit/size) vs pages/over-indicium.vue:20-24,50 (verbatim re-inline incl. `.carousel-fallback` CSS). → Extend HomeImageCarousel props (alt/fit/slidesPerView/height passthrough) or move fallback into ImageCarousel; consume from over-indicium; delete inline copies. Impact M / Effort S → acceptance: - [ ] one SSR-safe carousel wrapper; over-indicium reuses it
- TD-024 [MED] middleware/pdf-redirect.global.ts:15-25 (`.global`, UAParser on every navigation) vs pages/Statuten.vue:2-5, pages/HR.vue:2 — middleware re-encodes PDF paths owned by page `PdfViewer` srcs and drifted on 2/3 entries, so iOS Safari reads older statuten/HR than desktop; comment describes `navigateTo`, code does `window.location.href`. → iOS branch inside PdfViewer reusing the same src (or page-scoped middleware); delete legacy PDFs after confirming. Impact M / Effort S → acceptance: - [ ] iOS and desktop serve the same document; no global UA sniffing
- TD-026 [MED] content/navigation.yml:18-26 (hand-maintained Partners children) vs content/partners/*/index.md — still lists deleted DressMe (lands on "Partner niet gevonden" fallback, `pages/partners/[partner].vue:63-66`), omits current main partner, rots on every rename. → Derive children from the partners collection or add a nav-URL→content link-integrity check; remove DressMe now. Impact M / Effort S-M → acceptance: - [ ] no dead menu entries; partner add/remove needs no nav edit
- TD-027 [MED] app.vue:24-57 vs layouts/default.vue:11-56 — unscoped global CSS duplicated with conflicting values (#app sizing, scrollbar widths, `.page-content` min-height vs flex-grow); default.vue also reaches into nav internals (`body .nav`); minimal-layout pages inherit a different subset. → Shared base rules once in app.vue or `assets/css/main.css`; layout CSS scoped to its own structure. Impact S / Effort S → acceptance: - [ ] one base-rule source; no cross-file visual-regression risk
