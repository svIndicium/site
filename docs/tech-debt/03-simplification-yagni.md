# 03 — Simplification / YAGNI (delete dead code)

Batch as one deletion PR where convenient (TD-030/031/032/034/035/038/039 are independent, near-zero-risk removals).

- TD-030 [LOW] pages/playground.vue:1-315 — dev-only component sandbox ("Component playground", self-described at :141-144), zero references in nav/footer/content/components/tests/README, yet prerendered to prod hosting (`38.5KB` page + payload + CSS chunk). Owner (2026-09-06) actively uses it for container-query work → KEEP, but gate to dev-only (route rule/prerender exclusion or dev-only redirect) so it stops shipping to prod. Impact S / Effort S → acceptance: - [ ] no `/playground` in prod build; still usable in dev
- TD-031 [MED] error.vue:2-6 + pages/error.vue:2-6 (byte-identical; read only `route.query`, default cat-406, hotlinked httpcats image; ignore Nuxt `error` prop; zero showError/createError callers) + pages/[...slug].vue (third error surface, joke 404, shares stray `.face`/svg CSS) + firebase `**→index.html` rewrite making unknown URLs soft-404 home HTML. → Delete `pages/error.vue`; rewrite root error.vue from the error prop (real status/message, back link, no external image); one 404 strategy (`[...slug]` → showError for real 404 status). Impact M / Effort M → acceptance: - [ ] one error surface; real statuses; no soft-404s
- TD-032 [LOW] components/LoadingState.vue:1-45 (5.0KB branded SVG spinner) — zero references repo-wide, not Studio-global, no Suspense/async consumer. → Delete (wire into a real pending state only if one needs it). Impact S / Effort S → acceptance: - [ ] file gone; nothing imports it
- TD-033 [LOW] composables/useAppState.ts:10-29 (`useTheme` thin wrapper adding `preference`/`value`/`isDark`) — sole consumer `components/AppFooter.vue:2` destructures only `preference`; `AgendaCalendarButton.vue:12` already uses bare `useColorMode`. → Drop useTheme; call useColorMode directly. Impact S / Effort S → acceptance: - [ ] wrapper gone; footer behavior unchanged
- TD-034 [LOW] content.config.ts:91-96 (`about` collection targeting nonexistent `content/over-indicium.md`, self-documented unused, still surfaced in Studio) + content/links.d.ts:1-8 (unreferenced Content-v2-era types) + `src/content`, `src/views` (empty pre-Nuxt leftovers; no src↔root file duplication exists — verdict: nothing to merge). → Delete all three, or make over-indicium a real content file. Impact S / Effort S → acceptance: - [ ] no dead collection/types/dirs; Studio shows only real collections
- TD-035 [LOW] package.json:20 + nuxt.config.ts:8 + taze.config.js:16 — `@vueuse/nuxt` has zero app-code hits (exhaustive API-name + import grep; only skill-docs mention it). → Remove dep + module registration + taze entry; re-add on first real use. Impact S / Effort S → acceptance: - [ ] module out of build; lockfile pruned
- TD-036 [LOW] assets/logo/ (`logo.png` 260.2KB duplicating `public/logo.png`; `indicium-logo-top/dark/left` variants with zero refs — only `indicium-logo-icon.svg` is imported) — `public/logo/indicium-logo-top.svg` is externally pinned (mailchimp emails per `public/logo/README.md`) and stays. → Delete unreferenced `assets/logo` files except the icon. Impact S / Effort S → acceptance: - [ ] ~500KB bloat gone; pinned email asset untouched
- TD-037 [LOW] components/content/HomePartners.vue:1-61 — zero consumers (no MDC tag in content, no page import; only `nuxt.config.ts:193` registration) + tier rendering triplicated (`:27-43` despite existing `allPartners`). → Place it on the homepage (data layer ready via usePartners) or drop component + registration; collapse tier v-for either way. Impact S / Effort S → acceptance: - [ ] component live or deleted; no dead Studio picker entry
- TD-038 [LOW] pnpm-workspace.yaml:10-19 — `onlyBuiltDependencies` is the pnpm-10 schema, unread under pinned pnpm 11 (`package.json:50`); redundantly repeats allowBuilds names plus `@sentry/cli`/`vue-demi`, absent from the lockfile. → Delete block; keep allowBuilds. Impact S / Effort S → acceptance: - [ ] single build-approval schema; installs unchanged
- TD-039 [LOW] content/partners/chipsoft/index.md:31 — trailing stray `# test` heading rendered by ContentRenderer on live `/partners` premium block and `/partners/chipsoft`. → Delete the line. Impact S / Effort S → acceptance: - [ ] no "test" heading on prod partner pages
