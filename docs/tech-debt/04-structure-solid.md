# 04 — Structure / SOLID (boundaries)

- TD-040 [MED] plugins/router.client.ts:1-9 — `afterEach` stamps `document.title = 'Indicium - ' + to.name` (raw slugs like `partners-slug`), racing the 3 pages with real `useSeoMeta` titles (`Contact.vue:2-5`, `over-indicium.vue:2-5`, `[partner].vue:15-18`) and diverging from prerendered SSG titles post-hydration. → Delete plugin; per-page useSeoMeta + global `titleTemplate` in `nuxt.config.ts` app head. Impact M / Effort S-M → acceptance: - [ ] titles stable from first paint through navigation; no slug titles in SERPs
- TD-041 [MED] components/MenuItem.vue:22-27,53-61,66-77 — submenu `<ul>` renders only while hover-driven `shouldDisplayDropdown`; state set via mouseover + raw `addEventListener` in `onMounted` (bypassing Vue, duplicating template events). Keyboard/SR users can focus top-level links but never reach submenu items. → Drive state via focusin/focusout or `:focus-within` + `aria-haspopup`/`aria-expanded`. Impact M / Effort S-M → acceptance: - [ ] second-level nav keyboard-reachable with expanded-state announced
- TD-042 [MED] pages/*.vue (15 collection pages: `index.vue:2`, `Commissies.vue:2`, `Dispuut.vue:2`, `Bestuur.vue:4`, `besturen.vue:7`, `Vcp.vue:2`, `Links.vue:12`, `Contact.vue:7`, partners pages…) — every `useAsyncData` destructures only `{ data }`; no page reads `error`/`pending`, no pending/error UI in pages/layouts, so a misconfigured Studio collection renders blank/half-empty pages silently (`Links.vue:13` non-reactive snapshot vs `Contact.vue:9` computed — two conventions for the same shape). → One page recipe: destructure error/pending, empty-fallback via ErrorDisplay/ContentContainer, always `computed` over data. Impact M / Effort S-M → acceptance: - [ ] broken collection shows feedback, never blank; one data convention
- TD-043 [MED] utils/conscriboForm.ts:1-46 — six sequential silent `.replace(/regex/,…)` patches against remote Conscribo vendor JS with no per-pattern match assertion; a vendor update no-ops every fix (name validation, date-input class) while only fetch errors warn, so the membership form can regress silently; single caller (`ConscriboForm.client.vue:13`), zero testability. → Export pure patch function (regex list, fixture-feedable) that throws/logs loudly per unmatched pattern; keep fetch + DOM injection thin in the component. Impact M / Effort S-M → acceptance: - [ ] vendor drift fails loudly; patches unit-tested
- TD-044 [MED] components/PartnerLogo.vue:27-33 + components/content/HomeTextBlock.vue:53-60 (brightness magic numbers) + `components/SocialSidebar.vue:113`, `pages/Bestuur.vue:150-152` — dark tweaks key only on JS-set `html[data-theme='dark']`, but tokens also flip via OS media query and the no-JS footer-radio `:root:has()` path (`variables.css:149-150`), which never sets the attribute → wrong logo variant + unstyled hacks in an otherwise-dark page; brightness filters re-implement existing `--link-color` tokens. → Match the radio path too (`html:has(#dark-mode:checked)`) and/or fold tweaks into tokens. Impact M / Effort S → acceptance: - [ ] all four dark sources render identically; no magic-number filters
- TD-045 [MED] .github/workflows/*.yml + package.json:6-15 (no `lint` script; eslint fully configured via `eslint.config.mjs` + `@nuxt/eslint` module yet never executed) + test/ (only `test/unit/agenda.test.ts`, `test/nuxt/ActivityCalendar.test.ts`; gaps: `sanitizeAgendaMaxPages`, `resolveAgendaLocation` overrides, `formatAgendaDay/Month`, multiday-end branches, prev-disabled state, location hrefs; no coverage config) + `.nuxtrc:1` second untracked `@nuxt/test-utils` pin. → Add `lint` script + lint/test CI steps; add the listed cases if behaviors matter; remove or sync the `.nuxtrc` pin. Impact M / Effort S → acceptance: - [ ] CI runs tests + lint on every PR; versions pinned once
- TD-046 [MED] .github/workflows/firebase-hosting-merge.yml:9-21 + `-pull-request.yml:9-21` — `corepack prepare pnpm@latest` overrides the pinned `pnpm@11.21.0+sha512` (lockfile/workspace `allowBuilds` schema validated against 11); actions float on mutable tags (`checkout@v7`, `action-hosting-deploy@v0`), contradicting the repo's own taze `githubActions: sha` policy; no pnpm-store cache. → Honor the packageManager pin, taze action refs to SHAs, optionally cache the store. Impact M / Effort S → acceptance: - [ ] reproducible installs; pinned actions; faster CI
